import {styled, XStack} from "tamagui";

export const TrackerPreviewContainer = styled(XStack, {
  width: "100%",
  justifyContent: "space-between",
  animation: "200ms",
  pressStyle: {
    backgroundColor: "lightgrey",
  }
})